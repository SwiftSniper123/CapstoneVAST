#include "HighestZValue.h"


/*
HighestZValue::HighestZValue() : ScenarioMetric()
{
	//get number of AVs
	numAVs = VASTObject->AVs.size();
	value = -1.0*INFINITY;
	name = "HighestZValue";
}


HighestZValue::~HighestZValue()
{
	for (int n = 0; n < numAVs; n++)
	{
		if (AVs[n]->position.z > value)
			value = VASTObject->AVs[n]->position.z;
	}
}*/
