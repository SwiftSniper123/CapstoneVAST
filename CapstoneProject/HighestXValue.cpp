#include "HighestXValue.h"


/*
HighestXValue::HighestXValue(VAST *VASTObject) : ScenarioMetric(VASTObject)
{
	//get number of AVs
	numAVs = VASTObject->AVs.size();
	value = -1.0*INFINITY;
	name = "HighestXValue";
}


HighestXValue::~HighestXValue()
{
	for (int n = 0; n < numAVs; n++)
	{
		if (VASTObject->AVs[n]->position.x > value)
			value = VASTObject->AVs[n]->position.x;
	}
}
*/